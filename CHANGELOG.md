# 更新日志

本项目采用语义化版本号。

## 1.9.1 - 2026-08-28

- 修复主动推送被企微拒绝时可能触发未处理 Promise 并导致进程退出的问题。
- 正确解析 SDK 返回的对象型错误，响应中显示 `errcode`、`errmsg` 和 `hint`。
- 主动推送失败信息增加 `bot_key`、目标类型及实际使用的 `userid`/`chatid`，便于排查目标 ID。
- 批量推送失败项现在会返回明确错误信息，不再出现 `error: undefined`。

## 1.9.0 - 2026-08-28

- 支持在同一进程中配置并连接多个企业微信机器人。
- 每个机器人支持独立名称、用户白名单、群聊开关与群聊白名单。
- 主动触发 API 增加 `bot_key`，响应中返回实际发送机器人。
- 管理页面增加机器人选择和多连接状态展示。
- 对话上下文、消息去重、媒体收发与主动推送按机器人隔离。
- 保持原有单机器人环境变量向后兼容。

## 1.8.0 - 2026-08-27

- 增加内置企微消息触发器管理页面，参数可直接在浏览器填写。
- 支持粘贴多个 `userid`/`chatid`，以及上传 XLSX、CSV、TSV、TXT 批量导入。
- 增加带并发限制和逐目标结果的 `POST /api/trigger/batch` 接口。
- 表格导入和批量发送继续使用原有 Token 鉴权、目标白名单与媒体安全限制。
- 管理页面不会保存触发器 Token，刷新页面后自动清除。

## 1.7.1 - 2026-08-27

- 修复触发器切换 `userid`/`chatid` 后仍使用旧 `target_id` 的问题。
- 修复界面复用 `request_id` 时错误返回第一次目标且不再发送的问题。
- 幂等去重现在按“请求 ID + 请求内容”判断，内容变化会作为新请求执行。

## 1.7.0 - 2026-08-26

- 增加带 Token 鉴权的主动消息触发接口 `POST /api/trigger`。
- 支持按 `userid`、群 `chatid` 或通用目标字段主动发送消息。
- 支持文本、Markdown、图片、文件、语音、视频、模板卡片和 AI 生成模式。
- 增加请求幂等、媒体大小限制、目标白名单和本机监听默认值。
- 增加主动触发接口文档和 PowerShell 示例。

## 1.6.5 - 2026-08-26

- 完善企微图片输入、视觉模型调用和 Open WebUI 生成图片转发。
- 修复私聊与群聊图片提取、短时效链接和后台任务等待问题。
- 增加用户及群聊白名单、短期上下文和运行状态命令。
