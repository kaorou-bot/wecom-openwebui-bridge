{
  "name": "wecom-openwebui-bridge",
  "version": "1.9.0",
  "private": true,
  "type": "module",
  "description": "Enterprise WeChat AI Bot to Open WebUI bridge",
  "license": "MIT",
  "keywords": [
    "wecom",
    "open-webui",
    "chatbot",
    "enterprise-wechat",
    "ai-bot"
  ],
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check bot-config.js && node --check trigger-utils.js && node --check spreadsheet-import.js && node --check public/app.js",
    "test": "node --test"
  },
  "dependencies": {
    "@wecom/aibot-node-sdk": "1.0.7",
    "dotenv": "17.4.2"
  },
  "engines": {
    "node": ">=20"
  }
}
